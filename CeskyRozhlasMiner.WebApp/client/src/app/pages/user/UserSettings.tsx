import { Typography } from '@mui/material';
import React from 'react';

const UserSettings: React.FC = () => {
    return <Typography variant="body1">Settings</Typography>;
};

export default UserSettings;